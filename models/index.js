// const Reaction = required('./Reaction') This is only Schema, no table to being used for reaction
const Thought = require('./Thought')
const User = require('./User')

module.exports = { User, Thought }